### 思路

python默认是最小堆

即
heappop(heap)弹出当前最小值，
heappush(heap,val)插入元素并自动维护最小堆结构

如果想要实现最大堆，可以传入-val
因此我对于最大堆，我们可以手动写两个压入和弹出函数
```python
    def heappush_max(self,heap,val):
        heapq.heappush(heap,-val)

    def heappushpop_max(self,heap,val):
        self.heappush_max(heap,val)
        return -heapq.heappop(heap)
```

思路是：
中位数应该考虑左右两部分，右边的最小值大于左边的最大值，因此右边可以维护一个最小堆，左边可以维护一个最大堆

```python
class MedianFinder:

    def __init__(self):
        self.left=[]
        self.right=[]

    def heappush_max(self,heap,val):
        heapq.heappush(heap,-val)

    def heappushpop_max(self,heap,val):
        self.heappush_max(heap,val)
        return -heapq.heappop(heap)

    def addNum(self, num: int) -> None:
        # 如果左右元素个数相等，把新元素压进右边并弹出最小值，把弹出的最小值压进左边
        if len(self.left)==len(self.right):
            tem=heapq.heappushpop(self.right,num)
            self.heappush_max(self.left,tem)
        # 如果不相等，压入左边，弹出左边最小值，最小值压入右边
        else:
            tem=self.heappushpop_max(self.left,num)
            heapq.heappush(self.right,tem)

    def findMedian(self) -> float:
        if len(self.left)==len(self.right):
            return (self.right[0]-self.left[0])/2
        else:
            return -self.left[0]
```