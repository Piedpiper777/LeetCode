### 思路

```python
class Solution:

    def pivotIndex(self, nums: List[int]) -> int:

        left=0
        right=len(nums)-1
        s1=0
        s2=sum(nums)

        for i in range(len(nums)):
            s2-=nums[i]
            if s2==s1:
                return i
            s1+=nums[i]

        return -1
```