### 思路
动态规划的思路

对于当前台阶i，可以从i-1上来，也可以从i-2上来，所以总路径是
`到达i-1的路径数+到达i-2的路径数`
转化为斐波那契数列

```python
class Solution:

    def climbStairs(self, n: int) -> int:

        a=1 # 第0级台阶
        b=1 # 第1级台阶

        for _ in range(n-1):
            a,b=b,a+b

        return b
```