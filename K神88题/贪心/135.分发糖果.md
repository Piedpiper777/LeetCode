先对每一个人发一个

然后遍历，因为相邻所以需要考虑左右两边，如果一次遍历，同时考虑左右两边的话会很麻烦，所以其实可以两次遍历，从左到右比较当前元素和前置元素，从右到左比较当前元素和后置元素

```python
class Solution:

    def candy(self, ratings: List[int]) -> int:

        left=[1 for _ in range(len(ratings))]
        right=left[:]

        for i in range(1,len(ratings)):
            if ratings[i]>ratings[i-1]:
                left[i]=left[i-1]+1

        count=left[-1]

        for i in range(len(ratings)-2,-1,-1):
            if ratings[i]>ratings[i+1]:
                right[i]=right[i+1]+1
            count+=max(left[i],right[i])

        return count
```

- 时间复杂度：$O(N)$ ， 遍历两遍数组即可得到结果；
- 空间复杂度：$O(N)$ ，线性列表存储结果